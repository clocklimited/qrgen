{
  "author": "Paul Serby",
  "name": "QRGen",
  "description": "QR Code generator",
  "version": "2.0.1",
  "scripts": {
    "start": "node app",
    "test": "mocha -r should -R spec",
    "build": "echo No build needed on this project!",
    "prepare": "nave use 4 npm --registry http://npm.clockte.ch install && nave use 4 npm prune"
  },
  "dependencies": {
    "express": "^4.13.4",
    "marked": "^0.3.5",
    "morgan": "^1.7.0",
    "qrcode": "^0.4.2",
    "uber-cache": "^2.1.4",
    "uber-memoize": "^1.1.0"
  },
  "devDependencies": {
    "mocha": "^1.7.0",
    "request": "^2.12.0"
  }
}
